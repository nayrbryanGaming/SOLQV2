version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    depends_on:
      - redis
    networks:
      - SOLQ-net

  redis:
    image: "redis:alpine"
    networks:
      - SOLQ-net

networks:
  SOLQ-net:
    driver: bridge

